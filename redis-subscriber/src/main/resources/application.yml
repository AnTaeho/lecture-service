spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://localhost:3306/lecture
    username: root
    password: an902318

  jpa:
    #    show-sql: true
    properties:
      hibernate:
    #        use_sql_comments: true
    #        format_sql: true
    hibernate:
      ddl-auto: create

  redis:
    host: localhost
    port: 6379
    password:
